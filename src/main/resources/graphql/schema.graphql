# This file was generated. Do not edit manually.

schema {
    query: Query
}

type Anime {
    animeId: ID
    " Provider name"
    episodes: [Episode]
    " Anime ID"
    providerId: ID
    " Provider ID"
    providerName: String
}

type AnimeSource {
    audioResources: [AudioResource]
    " Subtitle type"
    audioType: String
    " Audio type"
    headers: [Header]
    providerId: ID
    " Provider ID"
    subtitleType: String
    subtitles: [Subtitle]
    " Headers"
    videoResources: [VideoResource]
}

type AudioResource {
    " Link to the audio"
    backupUrl: String
    " Backup link to the audio"
    quality: String
    url: String
}

type Episode {
    episodeLabel: String
    " Episode label"
    episodeNumber: Int
    " Episode number"
    value: String
}

type Header {
    key: String
    " Header key"
    value: [String]
}

type Query {
    animeSearch(id: ID): Result
    source(providerId: String, value: String): AnimeSource
}

type Result {
    " Success"
    animes: [Anime]
    " Number of results"
    success: Boolean
    totalResults: Int
}

type Subtitle {
    " Language of the subtitle"
    ass: String
    language: String
    srt: String
}

type VideoResource {
    " Server ID"
    audioQuality: String
    " Link to the video"
    backupUrl: String
    " Backup link to the video"
    quality: String
    " Quality of the video"
    serverId: ID
    url: String
    " Video type"
    useHeader: Boolean
    " Audio quality"
    videoType: String
}
